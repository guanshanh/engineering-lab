[project]
name = "langchain-lab"
version = "0.1.0"
description = "LangChain 工程实践项目"
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.7.6",
    "langchain-deepseek>=1.1.0",
    "python-dotenv>=1.2.3",
]
